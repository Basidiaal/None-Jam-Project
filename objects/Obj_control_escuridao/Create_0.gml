escuridao_alfa = 1.0; // 1.0 significa 100% visível
deve_sumir = false;   // Ativado quando o player abrir a porta
surf_escuridao = -1;
// Pega a largura e altura da câmera atual
cam_largura = camera_get_view_width(view_camera[0]);
cam_altura = camera_get_view_height(view_camera[0]);