// Garante que a camada de desenho seja do mesmo tamanho da janela
display_set_gui_size(window_get_width(), window_get_height());
texto = "";
sprite_fundo = noone;
margem = 60; // Espaço para o texto não bater na borda do papel